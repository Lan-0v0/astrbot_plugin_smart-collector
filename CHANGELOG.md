# Changelog

## v0.3.2 - 2026-08-02

- 在 Pixiv 自定义条目的“年龄段”下方增加画质选项：Auto、原图、大图和中图；默认 Auto 按原图、大图、中图顺序回退。
- 修复代理/TUN Fake-IP DNS 将 `i.pximg.net` 解析至 `198.18.0.0/15` 时被 SSRF 校验误拦截，导致所有 Pixiv 图片在发送前下载失败的问题；例外仅限 Pixiv 官方 CDN 和该专用 Fake-IP 网段。
- 拆分 Pixiv 永久去重、下载失败和图片校验错误，不再统一误报“作品均已去重或无法完整下载”。
- 将 Pixiv 永久去重补强为作品 ID + 页面内容双重记录，切换画质或回退规格后同一作品仍不会重复发送。
- 补充四种画质、Fake-IP 边界、下载错误诊断、画质切换去重、默认指令和发送链回归测试。

## v0.3.1 - 2026-08-02

- 修复默认 `/pixiv [Tag...]` 可能因 Pixiv 原图地址下载失败而误报“作品均已去重或无法完整下载”的问题。
- 为 Pixiv 原图增加大图和中图备用地址，下载失败时逐级回退，并在成功后继续按作品永久去重。
- 补充 `/pixiv 白丝 jk` 场景的多 Tag、备用图片地址和完整下载回归测试。

## v0.3.0 - 2026-08-01

- 审核并修复抓取器 SSRF 风险：拦截回环、私有、链路本地、保留地址及常见数字形式 IP，并在 DNS 解析、HTTPX/curl-cffi 每一跳重定向前校验目标。
- 统一 HTTPX、curl-cffi、HEAD/Range 探测和媒体下载的安全重定向处理，保留正常跨域跳转能力并限制最大跳转次数。
- 重构内置指令的精确匹配，确保新增条目不能覆盖 `/pixiv` 及登录指令；更新 Pixiv 专属帮助和配置提示。
- 修正配置中的字符串布尔值解析，限制限速、缓存清理和请求参数到配置面板约定范围。
- 加强 yt-dlp 视频入口和缓存清理路径校验，避免异常路径影响本地文件，并正确清理 PDF/ZIP 派生文件。
- 保留 Pixiv 默认指令、远程/本地登录、多页作品及 PDF/转发修复，并补充 SSRF、重定向和配置归一化回归测试。

## v0.2.2 - 2026-08-01

- 修复 PDF/ZIP 放入伪造转发节点后可能被 QQ 发送为 0B 文件的问题，文件改为直接上传。
- Pixiv 按作品随机选择并提前记录发送历史，避免同一 Tag 持续返回同一作品。
- 多页 Pixiv 作品会并发下载并整组发送；图片转 PDF 和压缩会包含作品的全部页面。
- 增加并发选择预留，避免多个请求同时选中同一未发送作品，并复用已缓存页面减少重复下载。

## v0.2.1 - 2026-08-01

- 增加 `/pixiv登陆 本地`，自动启动本机 Chrome/Edge 并通过 DevTools 协议截获 Pixiv OAuth code。
- 本地浏览器使用插件独立配置目录，不读取账号密码；登录完成后自动交换并保存 Refresh Token。
- 保留二维码及手动 URL/code 登录回退，支持嵌套回调地址解析并修复登录错误前缀重复。
- Pixiv Token 请求补充 Android User-Agent，提高 OAuth 接口兼容性。

## v0.2.0 - 2026-08-01

- 增加 Pixiv 自定义爬取项，支持全年龄、R18 和全部年龄段的多 Tag 图片搜索。
- 增加 `/pixiv` 帮助与 Tag 搜索指令，以及 `/pixiv登陆` 二维码 OAuth 登录和回调地址换取 Refresh Token。
- Pixiv 专属指令和自然语言请求可直接传入多个 Tag；Refresh Token 自动保存到插件数据目录。
- PDF 发送文件名固定为 `炸金~❤️.pdf`，并同步使用实际磁盘文件名。

## v0.1.7 - 2026-07-31

- 修复动态视频、音频和图片候选因复用原始 URL 缓存而重复发送首次内容的问题。
- 对随机、nonce、签名和过期参数等动态媒体地址重新请求，并继续按内容哈希复用真正相同的本地文件。
- 增加动态媒体 URL 回归测试，保留稳定 CDN 地址的缓存快路径。

## v0.1.6 - 2026-07-31

- 修复固定 URL 动态媒体端点命中来源缓存后一直返回首次内容的问题，Mukyu 等随机图片接口现在每次都会重新获取响应。
- 纯图片来源缺少文件扩展名时改为单次 GET 后按响应 MIME 识别，移除 HEAD/Range 探测产生的额外请求。
- 配置项本身为媒体直链时会重新验证内容，并继续通过内容哈希复用真正相同的文件；普通页面媒体候选的快速缓存逻辑不变。
- 增加固定随机入口连续抓取的回归测试，并完成 Mukyu 同缓存目录连续两次真实抓取验证。

## v0.1.5 - 2026-07-31

- 合并同 URL 候选并批量读取缓存与去重状态；缓存命中不再发起媒体探测，缓存回退不再逐条查询数据库。
- 图片探测已可靠确认小于配置阈值时直接跳过下载，并正确处理 Range 总大小、内容编码和旧缓存刷新；新下载图片不再重复读取文件校验。
- 详情页直链成功时不再提前请求嵌入播放器，失败时仍按原顺序启用完整回退。
- 复用 `curl-cffi` 浏览器指纹会话和连接池，加快同一运行期内的反爬回退请求。
- 仅爬取文字时跳过视频、音频和图片候选扫描，直接提取正文。

## v0.1.4 - 2026-07-31

- 在全局配置的“并发爬取数”上方增加“图片爬取忽略尺寸（KB）”输入框，默认忽略小于 100 KB 的图片，填写 `-1` 时关闭。
- 图片下载、直接响应和缓存复用均按磁盘实际文件大小执行阈值过滤；已知过小候选在探测排序时自动降级。
- 增加阈值边界、关闭开关、下载清理和配置顺序回归测试。

## v0.1.3 - 2026-07-31

- 将指令和 LLM 工具中的图片描述传入采集管线，支持主题词、排除词、横竖屏和最低分辨率约束。
- 增加通用图片候选评分：正文位置、`alt`、标题、图注、原图元数据、文件名和用户描述共同参与排序。
- 降低图标、Logo、头像、追踪像素、广告横幅和缩略图候选，保留低置信候选作为末级回退。
- 修复 `srcset` 未按规格选择的问题，扩展 Open Graph、JSON/API 原图字段、懒加载图片、CSS 背景图和相对/无扩展名图片 URL。
- 下载后验证实际图片格式、尺寸和占位图特征；跨域 CDN 返回占位图时自动携带 Referer 重试。
- 同时增强视频直链、DASH/HLS、Range 探测、候选分批回退、yt-dlp 页面下载和跨域播放器请求头隔离。

## v0.1.2 - 2026-07-31

- 修复 AstrBot 移除唤醒前缀后专属指令无法匹配的问题，`/看片` 等配置指令现在可正常触发。
- 改用自定义指令过滤器避免普通聊天误唤醒，并保护 `/爬取`、`/抓取` 不被自定义条目抢占。
- 直链媒体优先流式下载，HLS 与 `yt-dlp` 增加画质、请求头、Referer 回退及临时文件清理。
- 增强旧配置、异常 API 状态码和损坏解析画像的容错，修复定时发送失败仍标记成功的问题。
- PDF/ZIP 后处理改为原子写入，修复零天缓存清理的同时间戳边界问题。

## v0.1.1 - 2026-07-31

- 将 `/爬取` 固定为 `/爬取 [URL] [类型]`，缺少 URL 时只返回规范提示。
- 在每个爬取项中增加最低/最高视频画质偏好，并为 HLS、`yt-dlp` 和普通媒体候选提供自动回退。
- 在定时时间下方增加多项指定发送QQ群，通过 OneBot 会话主动发送并持久化发送时段。
- 将内置“Lanの默认配置”的消息伪造转发改为不启用，同时保持新增条目默认选择“用户”。
- 增加仅访问 Pektino 的快速联网烟测模式。

## v0.1.0 - 2026-07-31

- 将通用指令改为 `/爬取`，支持直接传入临时 URL 和自然语言内容类型。
- 将默认优先级调整为视频、图片、音频、文字，成功消息仅保留实际内容。
- 自动识别数字分页上限并随机翻页，支持 Pektino 的 7262 页列表。
- 修复 Next.js/JSON 转义媒体 URL，并在页面候选失败后继续详情页、嵌入页和 `yt-dlp` 回退。
- 媒体改为 HTTPX 与浏览器指纹回退的流式下载，移除 100 MiB 媒体缓冲限制。
- 跨域媒体先以最小请求头抓取，失败后再携带来源页 Referer 重试，兼顾防盗链与请求头隔离。
- 扩展 `srcset`、Open Graph、Twitter Card、延迟加载和脚本内多类媒体解析。
- 全局缓存清理覆盖 `/爬取 URL` 产生的动态数据源缓存。

## v0.0.2 - 2026-07-31

- 将并发爬取数和请求超时改为默认不限制的输入框，并实现 `-1` 的运行时语义。
- 将文字摘要、摘要人设和缓存清理改为自定义爬取项下方的全局设置。
- 将条目去重滑条范围调整为 `-1~0`。
- 添加配置结构与无限制抓取参数的回归测试。

## v0.0.1 - 2026-07-31

- 首次发布。
- 添加网站/API 配置模板、四类内容解析、缓存、去重、限速与并发抓取。
- 添加自适应解析画像、浏览器指纹回退和 `yt-dlp` 视频回退。
- 添加嵌入播放器、P.A.C.K.E.R. 配置与 HLS 实体视频解析，完成 Avbebe 在线烟测。
- 添加专属指令、LLM 工具、摘要、PDF、ZIP、伪造转发与定时发送。
- 添加单元测试、联网烟测和 GitHub Actions。
